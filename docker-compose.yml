services:

  backend:
    build:
      context: ./backend
    container_name: docker-ci-backend

    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: docker-ci-frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend